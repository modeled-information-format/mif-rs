---
title: mif-rs
template: splash
description: "mif-rs — a Rust implementation of MIF: schema validation, ontology resolution, lossless markdown/JSON-LD projection, local embeddings, and vector search, exposed as a CLI and an MCP server."
hero:
  tagline: A Rust engine for MIF — the same document a person reads and a parser reads, kept losslessly in sync, validated, embedded, and searchable.
  image:
    html: |
      <svg viewBox="0 0 560 480" width="560" height="480" fill="none" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="mif-rs — a Rust implementation of MIF. A chevron-M mark of two halves, machine-cyan and human-amber, meeting at one node over a dark field, above its four core crates: schema, ontology, embed, store.">
        <title>mif-rs — a Rust implementation of MIF</title>
        <desc>The chevron-M mark, the mif-rs wordmark, the line "Validate. Resolve. Embed. Search.", and its four core crates.</desc>
        <defs>
          <linearGradient id="mif-rs-home-field" x1="0" y1="0" x2="560" y2="480" gradientUnits="userSpaceOnUse">
            <stop offset="0" stop-color="#0A0D13"/>
            <stop offset="1" stop-color="#0E121B"/>
          </linearGradient>
        </defs>
        <rect width="560" height="480" rx="20" fill="url(#mif-rs-home-field)"/>
        <rect x="12" y="12" width="536" height="456" rx="14" fill="none" stroke="#222C3C" stroke-width="1.5"/>

        <g transform="translate(238 50) scale(1.55)" fill="none" stroke-width="5" stroke-linejoin="round" stroke-linecap="round">
          <path d="M6 42 L6 6 L24 29" stroke="#34D3E8"/>
          <path d="M24 29 L42 6 L42 42" stroke="#F5B642"/>
          <path d="M24 25.6 L27.4 29 L24 32.4 L20.6 29 Z" fill="#E8EEF6" stroke="none"/>
        </g>

        <text x="280" y="212" text-anchor="middle" font-family="Menlo, 'SF Mono', 'JetBrains Mono', monospace" font-size="46" font-weight="700" letter-spacing="0.02em" fill="#E8EEF6">mif-rs</text>
        <text x="280" y="246" text-anchor="middle" font-family="Menlo, 'SF Mono', 'JetBrains Mono', monospace" font-size="15" fill="#AEBCCF" letter-spacing="0.03em">A Rust implementation of MIF</text>
        <text x="280" y="292" text-anchor="middle" font-family="system-ui, -apple-system, 'Segoe UI', Helvetica, Arial, sans-serif" font-size="20" fill="#E8EEF6" letter-spacing="-0.01em">Validate. Resolve. Embed. Search.</text>

        <text x="280" y="356" text-anchor="middle" font-family="Menlo, 'SF Mono', 'JetBrains Mono', monospace" font-size="15" letter-spacing="0.01em">
          <tspan fill="#34D3E8">mif-schema</tspan><tspan fill="#7C8AA0">  ·  </tspan><tspan fill="#F5B642">mif-ontology</tspan>
        </text>
        <text x="280" y="382" text-anchor="middle" font-family="Menlo, 'SF Mono', 'JetBrains Mono', monospace" font-size="15" letter-spacing="0.01em">
          <tspan fill="#34D3E8">mif-embed</tspan><tspan fill="#7C8AA0">  ·  </tspan><tspan fill="#F5B642">mif-store</tspan>
        </text>

        <text x="280" y="438" text-anchor="middle" font-family="Menlo, 'SF Mono', 'JetBrains Mono', monospace" font-size="13" fill="#7C8AA0" letter-spacing="0.01em">modeled-information-format.github.io/mif-rs</text>
      </svg>
  actions:
    - text: Error reference
      link: /mif-rs/references/errors/
      icon: right-arrow
    - text: Rust API docs
      link: /mif-rs/rustdoc/mif_core/
      icon: right-arrow
      variant: minimal
    - text: GitHub
      link: https://github.com/modeled-information-format/mif-rs
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

`mif-rs` implements the [MIF (Modeled Information Format)](https://mif-spec.dev)
specification as nine Cargo workspace crates: seven libraries and two thin
binaries built on top of them.

<CardGrid>
	<Card title="Validate" icon="approve-check">
		`mif-schema` validates MIF documents, citations, and ontology
		definitions against the canonical, vendored JSON Schema — offline,
		no network access at validation time.
	</Card>
	<Card title="Resolve ontologies" icon="puzzle">
		`mif-ontology` resolves the three-tier `extends` inheritance chain
		across a corpus of ontology definition files.
	</Card>
	<Card title="Project losslessly" icon="document">
		`mif-frontmatter` projects markdown frontmatter to JSON-LD and back,
		proving the round trip is lossless — the frontmatter markdown is
		source of truth; JSON-LD is always a derived projection.
	</Card>
	<Card title="Embed and search" icon="magnifier">
		`mif-embed` computes local sentence embeddings offline after first
		fetch; `mif-store` ranks them by cosine similarity in a `SQLite`-backed
		vector store.
	</Card>
</CardGrid>

Both `mif-cli` and `mif-mcp` are thin consumers of those five libraries: a
command-line tool and an MCP server exposing the identical
validate/ingest/search/resolve operations as tools for an agent.

## Errors are a first-class, machine-readable surface

Every failure across the workspace maps to an
[RFC 9457](https://www.rfc-editor.org/rfc/rfc9457) `application/problem+json`
envelope with a stable, versioned `type` URI — not just a human-readable
message. See the [error reference](/mif-rs/references/errors/) for the full catalog.
