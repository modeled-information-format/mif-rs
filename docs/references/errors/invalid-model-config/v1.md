---
title: "Model config.json is not valid or unexpected"
description: "The model's config.json was not valid JSON or did not match the expected BERT config shape."
---

**Type URI:** `https://modeled-information-format.github.io/mif-rs/references/errors/invalid-model-config/v1`
**HTTP status:** 500
**Exit code:** 1
**Emitted by:** `mif-embed` (`EmbedError::Config`)

## Message

```
failed to parse model config: {0}
```

## Cause

The fetched `config.json` for `sentence-transformers/all-MiniLM-L6-v2` was
not valid JSON, or did not match the BERT config shape this crate expects.

## Suggested fix

**Unspecified** — this indicates an internal problem with `mif-embed` or the
cached model files. Try clearing the model cache directory and retrying, or
report it upstream if the problem persists.
