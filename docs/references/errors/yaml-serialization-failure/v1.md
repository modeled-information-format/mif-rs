---
title: "Internal error serializing frontmatter to YAML"
description: "Failed to serialize frontmatter back to YAML."
---

**Type URI:** `https://modeled-information-format.github.io/mif-rs/references/errors/yaml-serialization-failure/v1`
**HTTP status:** 500
**Exit code:** 1
**Emitted by:** `mif-frontmatter` (`FrontmatterError::YamlSerialize`)

## Message

```
failed to serialize frontmatter to YAML: {source}
```

## Cause

`serialize_markdown` failed to serialize an in-memory frontmatter mapping
back to YAML text. Not reachable through this crate's public functions with
a legitimately constructed input — indicates a bug in `mif-frontmatter`.

## Suggested fix

**Unspecified** — this indicates a bug in `mif-frontmatter`. Report it
upstream.
