---
title: "Document does not satisfy the requested level floor"
description: "A MIF document passed core schema validation but is missing fields an L1/L2/L3 level floor requires."
---

**Type URI:** `https://modeled-information-format.github.io/mif-rs/references/errors/level-floor-violation/v1`
**HTTP status:** 422
**Exit code:** 5
**Emitted by:** `mif-schema` (`MifSchemaError::LevelFloorViolation`); also surfaced verbatim through `mif-cli`'s and `mif-mcp`'s `Schema` wrapper variant

## Message

```
document does not satisfy the L{level} level floor: missing {joined field names}
```

## Cause

The instance passed to `validate_level` conformed to the canonical core MIF
JSON Schema, but is missing one or more fields the requested level floor
(L1, L2, or L3) additionally requires. Level floors are additive: L2 adds
`namespace`, `modified`, `temporal`; L3 adds `provenance` and a non-null
`temporal.validFrom`. L1 adds nothing beyond the core schema.

## Suggested fix

**MaybeIncorrect** — add the missing fields the requested level floor
requires, then retry.
