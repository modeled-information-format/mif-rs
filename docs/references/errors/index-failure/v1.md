---
title: "A SQLite index operation failed"
description: "A SQLite index operation failed."
---

**Type URI:** `https://modeled-information-format.github.io/mif-rs/references/errors/index-failure/v1`
**HTTP status:** 500
**Exit code:** 1
**Emitted by:** `mif-rh` (`MifRhError::Index`)

## Message

```
sqlite index operation failed: {source}
```

## Cause

A `SQLite` index operation failed.

## Suggested fix

**Unspecified** — this indicates a corrupt or inaccessible index database.
Delete it and rebuild with `mif-rh-cli review --build-index`, then retry.
